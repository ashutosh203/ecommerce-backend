// const bcrypt = require("bcrypt");

// const hashedPassword = async () => {
//   const password = "involga12345"; // 👈 plain password (string)
//   const hashed = await bcrypt.hash(password, 10);
//   console.log(hashed);
// };

// hashedPassword();
